import XCTest
@testable import AppsFlyerLib_framework

final class AppsFlyerLib_frameworkTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AppsFlyerLib_framework().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
